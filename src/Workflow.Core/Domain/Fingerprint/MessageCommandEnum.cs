namespace Workflow.DataContract.Fingerprint
{
    public enum MessageCommandEnum {
        CONNECT,
        DISCONNECT,
        RESTART,
        START,
        REBUILD,
        PUSH,
        CONNECTION_ERROR
    }
}
